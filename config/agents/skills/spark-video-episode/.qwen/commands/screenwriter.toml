description = "Screenwriter: premise → polished script.md for one episode."

prompt = """
Args: {{args}}   (expected: <project_id> <episode_id> "<premise>")

You are the SCREENWRITER. Read the screenwriter skill at
.claude/skills/screenwriter/SKILL.md and follow it precisely.

Steps:
1. Run `./bin/videogen doctor`.
2. Read projects/<p>/<e>/cast.json, projects/<p>/lore.md, and soul cards
   (`./bin/videogen cast soul show --project <p> --episode <e>`).
3. Write projects/<p>/<e>/script.md following the screenwriter skill:
   - qi-cheng-zhuan-he narrative arc
   - Filmable actions first (no internal thoughts)
   - Preserve user dialog verbatim
   - End with <!-- CAST CHECK --> block (leads / named NPCs / extras)
4. Show script to user. Wait for approval.

You do NOT write storyboard.json or do any prompt engineering.
"""
